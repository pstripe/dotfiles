gui:
  filterMode: 'fuzzy'
  nerdFontsVersion: '3'
git:
  diffRenderers:
    - command: delta --paging=never --dark --line-numbers --hyperlinks --hyperlinks-file-link-format="lazygit-edit://{path}:{line}"
